<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="AboutMapper">

	<select id="selectByTitle" parameterType="String" resultType="About">
		SELECT * FROM about
		WHERE 1 = 1 AND title = #{title}
	</select>

	<!--<update id="updateByTitle" parameterType="About">-->
		<!--UPDATE about-->
		<!--SET author  = #{author},-->
			<!--content = #{content},-->
			<!--date    = #{date}-->
		<!--WHERE 1 = 1 AND title = #{title}-->
	<!--</update>-->

</mapper>